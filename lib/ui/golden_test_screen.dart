import 'package:flutter/material.dart';

class GoldenTestScreen extends StatelessWidget {
  const GoldenTestScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Golden Test'),
      ),
      body: const Text(''),
    );
  }
}
